apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-monitor
  namespace: argocd
spec:
  project: default  # Cambiado de kubeops a default
  source:
    repoURL: https://github.com/JaimeHenaoChallange/opcion2.git
    targetRevision: HEAD
    path: scripts/cronjob
  destination:
    server: https://kubernetes.default.svc
    namespace: poc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
